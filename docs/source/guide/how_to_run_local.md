# How to Run PMS Booking Service in Local Mode

This guide shows how to start the service locally, assuming dependencies are installed.

## Step 1: Start Kafka

```bash
./gradlew upKafka
````

## Step 2: Run the Application

```bash
./gradlew bootRun
```
